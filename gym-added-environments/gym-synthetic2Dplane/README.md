The Synthetic Environment for visualizing the efficiency of imitation learning using mixture of policies.

```
pip3 install -e .
```

Later use this env as following:

```
import gym
import gym_synthetic2Dplane
gym.make('synthetic2Dplane-v0')
```